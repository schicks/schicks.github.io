{
  "name": "schicks-github-io-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=website",
    "build": "npm run build --workspace=website",
    "preview": "npm run preview --workspace=website",
    "check": "npm run check --workspace=website",
    "check:watch": "npm run check:watch --workspace=website",
    "lint": "npm run lint --workspace=website",
    "format": "npm run format --workspace=website",
    "deploy": "npm run deploy --workspace=website"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "nx": "21.3.7"
  }
}